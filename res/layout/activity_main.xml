<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    tools:context=".MainActivity" >

	<!-- HorizontalScrollView的使用 -->
	<Button 
	    android:id="@+id/buttonHorizontalScrollView"
	    android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:background="#33FF88"
		android:text="HorizontalScrollView的使用"
		android:textSize="16sp"
		android:textColor="#FF0000"	/>
	
	<!-- 自定义HorizontalScrollView的使用 -->
	<Button 
	    android:id="@+id/myButtonHorizontalScrollView"
	    android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:background="#33FF88"
		android:text="自定义HorizontalScrollView的使用"
		android:textSize="16sp"
		android:textColor="#FF0000"	
		android:layout_marginTop="20dp"/>	
    

</LinearLayout>